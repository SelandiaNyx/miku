# common\decisions\miku_infrastructure_decisions.txt
miku_infrastructure_category = {
	miku_infrastructure = {
		icon = generic_decision
		desc = miku_infrastructure_desc
		# 改为手动选择地块的关键设置
		state_target = any_owned_state		# 只显示你拥有的地块（推荐）
		# state_target = yes               # 如果想显示所有地块（包括占领的），可改成这个
		available = {
			num_of_civilian_factories_available_for_projects > 2
		}
		target_trigger = {
			FROM = {
				is_owned_by = ROOT				# 确保只选自己拥有的地块
				infrastructure < 5				# 只显示基础设施未满级（上限通常为5）的地块
			}
		}
		cost = 50
		fire_only_once = no
		days_remove = 30
		modifier = {
			civilian_factory_use = 1
		}
		# 决议完成（30天后）时的效果：对玩家手动选择的地块执行 +1 基础设施
		remove_effect = {
			FROM = {
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes					# 立即完成，不进入建造队列
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 2
				has_political_power > 200
			}
		}
	}
}
